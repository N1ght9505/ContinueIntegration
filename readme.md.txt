#Proyecto Docker

Este proyecto crea dos contenedores Ubuntu:

- MySQL
- WebServer

Ambos contenedores se comunican mediante docker compose.

Para subir los contenedores

docker compose up -d

Para validar que se encuentran corriendo:

docker ps -a

